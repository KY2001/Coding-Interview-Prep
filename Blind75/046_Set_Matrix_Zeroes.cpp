#include <bits/stdc++.h>

using namespace std;

// 1 <= m, n <= 200
// -2^31 <= matrix[i][j] <= 2^31 - 1
voide solve(vector<vector<int>>& matrix){
    const int m = matrix.size();
    const int n = matrix[0].size();
    for (int i = 0; i < )
}

class Solution {
public:
    void setZeroes(vector<vector<int>>& matrix) {
        
    }
};
